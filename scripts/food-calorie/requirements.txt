# Install CUDA PyTorch first on Windows (CPU wheel is default on pip):
#   .venv\Scripts\python.exe -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# Then:
#   .venv\Scripts\python.exe -m pip install -r scripts/food-calorie/requirements.txt
ultralytics>=8.3.0
pillow>=10.0.0
tqdm>=4.66.0
pyyaml>=6.0.0
